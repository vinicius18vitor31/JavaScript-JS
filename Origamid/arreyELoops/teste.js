const videoGame = ['Nitendo', 'PS2', 'NeoGeo'];
console.log(videoGame);

for (let numero = 0; numero < 15; numero++) {
    console.log(numero);
};

const Game = ['Nitendo', 'PS2', 'NeoGeo', 'PS4'];
for(let item = 0; item < Game.length; item++){
    console.log(Game[item]);
};